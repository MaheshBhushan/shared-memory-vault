# Contributing

Keep Markdown authoritative, SQLite disposable, recall local, and adapters thin. Do not add embeddings, cloud dependencies, HTTP, or MCP without a measured compatibility requirement and prior discussion.

Run `python -m unittest discover -s tests -v` on changes. New platform claims need a real-platform test or an explicit unverified label.
